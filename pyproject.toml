[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "arabic-dictionary"
version = "0.2.1"
description = "A modern open-source Arabic dictionary toolkit."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"

authors = [
    {name = "Rami Sulaimani"}
]

dependencies = [
    "requests>=2.32",
    "mwparserfromhell>=0.7",
    "openpyxl>=3.1",
    "rich>=13",
]

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "network: marks tests that require network access (run with --run-network)",
]

[tool.mypy]
python_version = "3.11"
strict = true

files = [
    "src/arabic_dictionary/domain",
    "tests",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "black",
    "ruff",
    "mypy",
    "pre-commit",
    "types-requests",
]